# 给部署 agent 的说明

1. 先阅读 README、USAGE_POLICY、PRIVACY 和 DISCLAIMER。代码为标准 MIT，政策不作为 MIT 的附加限制。作者的国籍自述不意味着项目取得政府认可或任何内容授权。
2. 使用用户指定的空目录部署发行包。不得覆盖正在运行的私人实例、复制旧片库或导入其他人的账号。不得直接归档任何生产目录。
3. 默认 `mode=ordinary`，来源为空。只接入用户提供的 Bilibili、抖音链接；不要自动搜集或导入第三方片库。
4. 根据平台运行 `start-windows.bat setup` 或 `./start-ubuntu.sh setup`。Git 与源码包不包含网页构建产物；安装 Node.js 22.12+，由启动脚本在用户本机安装前端依赖并构建。
5. 需要分离音视频播放或云缓存时安装 FFmpeg；需要云存储时安装 rclone。先确认用户提供的远程名、目标目录和独立授权路径，不得默认读取系统 rclone 配置、浏览器 Cookie 或 Google/Gemini/Drive 登录状态。不要让用户在公开聊天或 Git 中粘贴密钥。
6. 启动 `web`，确认只监听 `127.0.0.1`。不要开放防火墙端口、设置公网反代、公开隧道或安装云端公共服务。需要私人访问时只使用用户自己的受控连接。
7. 验证首页、导入页面、空目录、热门页和缓存未配置提示。只有用户提供合法测试链接和自己的存储后，才验证该来源和云缓存；不要把测试目录或记录打包。
8. 用户明确要求“特殊模式”时，说明公开包只提供扩展接口，不含成人站点或成人爬虫。按 README 的手动加站步骤，让用户指定合法来源与页面/API 资料；在 `private/provider.py` 实现并审查 `collect(url, config)`、`resolve(video, config)`，按域名路由、设置网络超时及条数限制，先以 3 条数据验证，再设置本机 `mode=special`、`special_plugin` 和 `sources`。新增栏目需适配路径，新增站点需先扩展解析逻辑；只填网址不能自动适配任意网站。自定义插件的 Cookie 读取应显式实现。缺少资料时说明缺失项，不编造下载地址、不自动加入其他来源、不绕过平台权限。
9. 保留失败信息的必要摘要；输出前删除 URL 查询令牌、授权值、账号和私人路径。确认用户使用自己的授权，不使用作者的任何登录资料。
10. 发布前运行测试、本机构建验证和 `pack.py`。构建结果仅用于本地验证，不得上传或进入源码包；`web/out`、`dist`、依赖、运行数据和配置必须排除。核对清单、内容扫描、Git 历史和归档文件；旧 Git 历史可能含秘密时不要复用。对外报告实际验证范围，不把 Linux 测试说成 Windows 实机测试。
